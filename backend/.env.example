# First Story Backend - 环境变量模板（复制为 .env 后填入真实值）
# API key 仅从环境变量读取，绝不入库/回显/写日志。

# LLM provider 选择（默认 deepseek）
FIRST_STORY_LLM_PROVIDER=deepseek
# 你的 API key（DeepSeek 控制台获取）。切勿提交真实 key。
FIRST_STORY_LLM_API_KEY=
# 模型名：deepseek-v4-pro（默认）/ deepseek-v4-flash
FIRST_STORY_LLM_MODEL=deepseek-v4-pro
FIRST_STORY_LLM_BASE_URL=https://api.deepseek.com
# 外网调用代理；不需要代理则留空
FIRST_STORY_LLM_PROXY=http://127.0.0.1:7890
