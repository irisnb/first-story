# 待办任务 - 2026-06-22

> 计划使用 git worktree 多任务并行处理

## 任务一：检查新功能代码

**目标**：验证 Phase 1 + Phase 2 实现的正确性和完整性

**范围**：
- 聊天历史持久化
- 分页加载功能
- ContextSummary 数据模型
- 分层摘要生成服务
- 投影器事件处理

**检查项**：
- [ ] 后端测试全部通过
- [ ] 前端 TypeScript 编译通过
- [ ] 手动测试：聊天历史加载
- [ ] 手动测试：分页加载更多
- [ ] 代码审查：新增文件的代码质量
- [ ] 确认 API contract 文档需要更新

**相关文件**：
- `backend/app/models/state.py` — ContextSummary 模型
- `backend/app/services/context_summary.py` — 摘要服务
- `backend/app/services/event_log.py` — 聊天消息索引
- `backend/app/services/dialogue.py` — 轮数计数
- `backend/app/services/projector.py` — 事件处理
- `frontend/src/lib/chat-runtime-context.tsx` — 前端分页逻辑

---

## 任务二：前端 UI 更新

**目标**：让前端 UI 展示新功能

**子任务**：
- [ ] 聊天面板：显示"加载更多"按钮状态（加载中/已加载全部）
- [ ] 聊天面板：优化消息列表滚动行为
- [ ] 状态面板：显示 ContextSummary 摘要信息
- [ ] 设置面板：添加摘要更新频率配置（可选）

**设计考虑**：
- 分页按钮放在消息列表顶部
- 摘要信息可放在侧边栏或独立卡片
- 保持非侵入式设计原则

**相关文件**：
- `frontend/src/views/ChatPanel.tsx`
- `frontend/src/views/StatePanel.tsx`（或新建）
- `frontend/src/lib/api-types.ts`

---

## 任务三：五兄弟重构

**目标**：实现五模块（世界/角色/情节/主题/结构）的 MD 文档作为真相源

**设计文档**：`docs/五兄弟重构设计.md`

**子任务**：
- [ ] 创建 MD 文档解析器
- [ ] 实现分类 API（GET /world, /characters, /plot, /theme, /structure）
- [ ] 实现固定 section 结构
- [ ] 实现乐观锁机制
- [ ] 更新前端画布 UI 展示五模块
- [ ] 迁移现有数据到新格式

**技术要点**：
- MD 文档为真相源，事件日志为投影
- 每个 module 有固定的 section 结构
- 编辑时通过 revision + checksum 实现乐观锁

---

## 任务四：项目设置 API 配置模块

**目标**：在项目设置处添加 API 接口配置功能

**功能需求**：
- [ ] 用户可自定义 LLM API 端点
- [ ] 用户可切换不同模型（GPT-4, Claude, 本地模型等）
- [ ] 用户可配置 API Key
- [ ] 支持多个配置方案切换

**后端任务**：
- [ ] 创建 `ProjectConfig` 数据模型
- [ ] 实现 `GET/PUT /api/v1/projects/{id}/config` 端点
- [ ] 实现 API Key 安全存储（加密）
- [ ] 更新 LLMProvider 支持动态配置

**前端任务**：
- [ ] 创建设置面板 UI
- [ ] API 配置表单
- [ ] 模型选择下拉框
- [ ] 配置测试按钮

**安全考虑**：
- API Key 不明文存储
- 前端只显示 Key 的后4位
- 配置变更需要确认

---

## Worktree 并行策略

```bash
# 主工作区保持当前状态
cd "D:\Frist story"

# 任务一：代码检查
git worktree add "../first-story-check" master

# 任务二：前端更新
git worktree add "../first-story-frontend" master

# 任务三：五兄弟重构
git worktree add "../first-story-five" master

# 任务四：API配置
git worktree add "../first-story-config" master
```

**分支策略**：
- 每个任务在独立分支开发
- 完成后 PR 回 master
- 避免同时修改同一文件

---

## 优先级建议

| 任务 | 优先级 | 预估时间 | 依赖 |
|------|--------|----------|------|
| 任务一：代码检查 | P0 | 1-2h | 无 |
| 任务二：前端更新 | P1 | 2-3h | 任务一 |
| 任务三：五兄弟重构 | P2 | 4-6h | 无 |
| 任务四：API配置 | P3 | 3-4h | 无 |

**建议执行顺序**：
1. 先完成任务一（代码检查），确保基础稳固
2. 任务二和任务三可以并行（不冲突）
3. 任务四可以独立进行
